{
  "name": "monte-carlo-sprint-sim",
  "version": "1.0.0",
  "private": true,
  "description": "Monte Carlo forecast of sprint delivery odds (velocity and flow) from sprint history.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
